import { data } from "../data/data";

// SPACE DATA EXERCISE 3
// Return an array of all Planets' average temperatures
// Return example: [1, 2, ... , N]

export function getAllAverageTemperatures(data: number) {
  const allAvgTemp: [] = [];
    data.planets.forEach(avgTemp => {
        allAvgTemp.push();
    });
    return allAvgTemp;
}